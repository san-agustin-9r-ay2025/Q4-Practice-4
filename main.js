function openSesimei() {
    let counter = 18;
    const maxCounts = 25;

    while (counter < maxCounts) {
        window.alert(counter);
        counter += 1;
    }
    window.alert("Full parking!");
}